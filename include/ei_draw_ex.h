#ifndef EI_DRAW_EX_H
#define EI_DRAW_EX_H

ei_linked_point_t* convert_arc(const ei_point_t center,
                               const int radius,
                               const float first_angle,
                               const float last_angle);

#endif
